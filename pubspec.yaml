name: exchenge_mobile
description: Exchange mobile app (WebView)
publish_to: "none"
version: 1.1.2+13

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.3.0
  url_launcher: ^6.3.1
  connectivity_plus: ^6.1.0
  package_info_plus: ^8.0.2
  http: ^1.2.2
  path_provider: ^2.1.4
  open_filex: ^4.5.0
  pasteboard: ^0.5.0
  share_plus: ^10.1.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
